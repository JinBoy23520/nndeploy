# Git 提交指南 - 视频超分自定义节点

## 📦 需要提交的文件清单

### 1. Python 源代码（7个文件）

```bash
# 超分辨率节点
python/nndeploy/super_resolution/realesrgan.py       # 109 行
python/nndeploy/super_resolution/srresnet.py         # 234 行
python/nndeploy/super_resolution/opencv_superres.py  # 167 行
python/nndeploy/super_resolution/__init__.py         # 已修改

# 视频编解码节点
python/nndeploy/codec/video_player.py               # 130 行
python/nndeploy/codec/side_by_side_compare.py       # 171 行
python/nndeploy/codec/detail_zoom_compare.py        # 185 行
python/nndeploy/codec/__init__.py                   # 已修改
```

### 2. 工作流配置（11个文件）

```bash
resources/workflow/RealESRGAN视频超分.json
resources/workflow/SRResNet视频超分.json
resources/workflow/SRResNet 4倍超分.json
resources/workflow/脸部超分修复.json
resources/workflow/超分效果对比.json
resources/workflow/OpenCV超分对比.json
resources/workflow/EDSR超分对比.json
resources/workflow/ESPCN超分对比.json
resources/workflow/FSRCNN超分对比.json
resources/workflow/EDSR 4倍超分（原始尺寸）.json
resources/workflow/EDSR超分（局部细节对比）.json
```

### 3. 文档（3个文件）

```bash
视频超分效果说明.md
OpenCV超分模型下载指南.md
跨平台部署指南.md
提交代码指南.md  # 本文件
```

---

## 🚀 快速提交命令（Windows PowerShell）

### 方式1：一键提交所有文件

```powershell
# 激活虚拟环境（如果需要）
& D:/jinwork/nndeploy-1/venv311/Scripts/Activate.ps1

# 切换到项目根目录
cd d:\jinwork\nndeploy-1

# 添加所有自定义节点
git add python/nndeploy/super_resolution/realesrgan.py
git add python/nndeploy/super_resolution/srresnet.py
git add python/nndeploy/super_resolution/opencv_superres.py
git add python/nndeploy/super_resolution/__init__.py
git add python/nndeploy/codec/video_player.py
git add python/nndeploy/codec/side_by_side_compare.py
git add python/nndeploy/codec/detail_zoom_compare.py
git add python/nndeploy/codec/__init__.py

# 添加所有工作流
git add "resources/workflow/RealESRGAN视频超分.json"
git add "resources/workflow/SRResNet视频超分.json"
git add "resources/workflow/SRResNet 4倍超分.json"
git add "resources/workflow/脸部超分修复.json"
git add "resources/workflow/超分效果对比.json"
git add "resources/workflow/OpenCV超分对比.json"
git add "resources/workflow/EDSR超分对比.json"
git add "resources/workflow/ESPCN超分对比.json"
git add "resources/workflow/FSRCNN超分对比.json"
git add "resources/workflow/EDSR 4倍超分（原始尺寸）.json"
git add "resources/workflow/EDSR超分（局部细节对比）.json"

# 添加文档
git add 视频超分效果说明.md
git add OpenCV超分模型下载指南.md
git add 跨平台部署指南.md
git add 提交代码指南.md

# 提交
git commit -m "feat: 添加视频超分自定义节点和工作流

- 新增 RealESRGAN, SRResNet, OpenCVSuperRes 超分节点
- 新增 VideoPlayer, SideBySideCompare, DetailZoomCompare 显示节点
- 新增 11 个工作流配置文件
- 新增跨平台部署文档和使用指南
- 支持 EDSR/ESPCN/FSRCNN 轻量级 AI 模型
- 支持原始尺寸对比和局部细节放大对比"

# 推送到远程（替换 origin 为你的远程仓库名）
git push origin main
```

### 方式2：分步提交（更安全）

```powershell
# Step 1: 提交 Python 节点
git add python/nndeploy/super_resolution/*.py
git add python/nndeploy/codec/video_player.py
git add python/nndeploy/codec/side_by_side_compare.py
git add python/nndeploy/codec/detail_zoom_compare.py
git commit -m "feat: 添加视频超分自定义节点"

# Step 2: 提交工作流配置
git add resources/workflow/*超分*.json
git add "resources/workflow/脸部超分修复.json"
git commit -m "feat: 添加视频超分工作流配置"

# Step 3: 提交文档
git add *.md
git commit -m "docs: 添加视频超分使用和部署文档"

# Step 4: 推送
git push origin main
```

---

## 🔍 提交前检查

### 1. 查看待提交文件

```powershell
# 查看当前状态
git status

# 查看修改内容
git diff

# 查看已暂存文件
git diff --cached
```

### 2. 确认不提交的文件

```powershell
# 检查模型文件（应该被忽略）
Get-ChildItem -Recurse -Include *.pb, *.pth, *.onnx

# 检查虚拟环境（应该被忽略）
Test-Path venv311
Test-Path venv

# 检查构建产物（应该被忽略）
Test-Path build
```

### 3. 验证 .gitignore

```powershell
# 测试文件是否被忽略
git check-ignore -v resources/models/opencv_dnn_superres/EDSR_x2.pb
git check-ignore -v venv311/
git check-ignore -v __pycache__/

# 如果输出为空，说明文件会被提交（需要检查 .gitignore）
```

---

## ⚠️ 常见错误和解决方案

### 错误1: 文件路径包含中文

**问题**:
```
warning: LF will be replaced by CRLF in resources/workflow/EDSR 4倍超分（原始尺寸）.json
```

**解决**:
```powershell
# 设置 Git 自动处理换行符
git config core.autocrlf true

# 或使用双引号包裹文件名
git add "resources/workflow/EDSR 4倍超分（原始尺寸）.json"
```

### 错误2: 误提交大文件

**问题**:
```
remote: error: File resources/models/EDSR_x2.pb is 38.00 MB; this exceeds GitHub's file size limit of 100.00 MB
```

**解决**:
```powershell
# 从暂存区移除
git reset resources/models/EDSR_x2.pb

# 添加到 .gitignore
echo "*.pb" >> .gitignore
git add .gitignore
git commit -m "chore: 忽略模型文件"
```

### 错误3: 分支不存在

**问题**:
```
error: pathspec 'main' did not match any file(s) known to git
```

**解决**:
```powershell
# 查看当前分支
git branch

# 如果是 master 分支
git push origin master

# 或创建并切换到 main 分支
git checkout -b main
git push -u origin main
```

---

## 🍎 macOS 端拉取代码

在 Mac 电脑上执行：

```bash
# 克隆仓库（首次）
git clone https://github.com/your-username/nndeploy-1.git
cd nndeploy-1

# 或拉取更新（已有仓库）
cd nndeploy-1
git pull origin main

# 查看提交历史
git log --oneline -10

# 查看文件列表
find python/nndeploy/super_resolution -name "*.py"
find python/nndeploy/codec -name "*.py"
ls -lh resources/workflow/*超分*.json
```

---

## 📊 提交统计

### 代码统计

```powershell
# 统计新增行数
$files = @(
    "python/nndeploy/super_resolution/realesrgan.py",
    "python/nndeploy/super_resolution/srresnet.py",
    "python/nndeploy/super_resolution/opencv_superres.py",
    "python/nndeploy/codec/video_player.py",
    "python/nndeploy/codec/side_by_side_compare.py",
    "python/nndeploy/codec/detail_zoom_compare.py"
)

$totalLines = 0
foreach ($file in $files) {
    $lines = (Get-Content $file | Measure-Object -Line).Lines
    Write-Host "$file : $lines 行"
    $totalLines += $lines
}
Write-Host "总计: $totalLines 行代码"
```

**预期输出**:
```
realesrgan.py : 109 行
srresnet.py : 234 行
opencv_superres.py : 167 行
video_player.py : 130 行
side_by_side_compare.py : 171 行
detail_zoom_compare.py : 185 行
总计: 996 行代码
```

---

## 🎯 最佳实践

### 1. 提交信息规范

```bash
# 功能添加
git commit -m "feat: 添加 EDSR 超分节点"

# Bug 修复
git commit -m "fix: 修复 VideoPlayer 播放速度过快问题"

# 文档更新
git commit -m "docs: 更新跨平台部署指南"

# 性能优化
git commit -m "perf: 优化 OpenCVSuperRes 推理速度"

# 代码重构
git commit -m "refactor: 重构 SideBySideCompare 缩放逻辑"

# 样式调整
git commit -m "style: 格式化代码符合 PEP8 规范"
```

### 2. 分支管理

```powershell
# 创建功能分支
git checkout -b feature/video-super-resolution

# 完成后合并到主分支
git checkout main
git merge feature/video-super-resolution

# 删除功能分支
git branch -d feature/video-super-resolution
```

### 3. 标签管理

```powershell
# 打版本标签
git tag -a v1.0.0 -m "Release v1.0.0: 视频超分功能"

# 推送标签
git push origin v1.0.0

# 查看所有标签
git tag -l
```

---

## 📁 提交后的目录结构

```
nndeploy-1/
├── python/
│   └── nndeploy/
│       ├── super_resolution/
│       │   ├── __init__.py           ✅ 已提交
│       │   ├── realesrgan.py         ✅ 已提交
│       │   ├── srresnet.py           ✅ 已提交
│       │   └── opencv_superres.py    ✅ 已提交
│       └── codec/
│           ├── __init__.py           ✅ 已提交
│           ├── video_player.py       ✅ 已提交
│           ├── side_by_side_compare.py  ✅ 已提交
│           └── detail_zoom_compare.py   ✅ 已提交
├── resources/
│   ├── workflow/
│   │   ├── RealESRGAN视频超分.json   ✅ 已提交
│   │   ├── EDSR超分对比.json         ✅ 已提交
│   │   └── ...                      ✅ 已提交（11个文件）
│   └── models/
│       └── opencv_dnn_superres/
│           ├── EDSR_x2.pb           ❌ 不提交（38MB）
│           └── ...                  ❌ 不提交
├── 视频超分效果说明.md                ✅ 已提交
├── OpenCV超分模型下载指南.md          ✅ 已提交
├── 跨平台部署指南.md                  ✅ 已提交
├── 提交代码指南.md                    ✅ 已提交
├── .gitignore                        ✅ 已提交
├── venv311/                          ❌ 不提交（虚拟环境）
└── build/                            ❌ 不提交（构建产物）
```

---

## ✅ 完成检查清单

提交前请确认：

- [ ] 所有 7 个 Python 文件已添加
- [ ] 所有 11 个工作流 JSON 已添加
- [ ] 所有 4 个文档 Markdown 已添加
- [ ] `.gitignore` 已正确配置
- [ ] 模型文件（.pb, .pth）未添加
- [ ] 虚拟环境（venv311/）未添加
- [ ] 构建产物（build/）未添加
- [ ] 日志文件（logs/）未添加
- [ ] 提交信息清晰描述改动
- [ ] 已推送到远程仓库

---

## 🔗 相关命令速查

```powershell
# 查看状态
git status

# 查看提交历史
git log --oneline -10

# 查看文件差异
git diff python/nndeploy/codec/video_player.py

# 撤销暂存
git reset HEAD python/nndeploy/codec/video_player.py

# 撤销修改
git checkout -- python/nndeploy/codec/video_player.py

# 查看远程仓库
git remote -v

# 拉取最新代码
git pull origin main

# 推送到远程
git push origin main
```

---

## 🎉 完成

按照本指南操作后，您的代码将成功提交到 Git，可以在 macOS 或其他电脑上轻松部署！

**关键要点**：
1. ✅ 只提交源代码和配置文件
2. ❌ 不提交模型文件（体积大）
3. ❌ 不提交虚拟环境（平台差异）
4. ✅ 使用 .gitignore 自动排除
5. ✅ 提交信息规范清晰
